[pytest]
testpaths = tests
# Run from RL-encoder/ directory so local packages (egraph_encoder, model, etc.) resolve
# without installation. Also adds Learn-R to sys.path for egraph_bridge imports.
pythonpath = . ../Learn-R
addopts = --tb=short --ignore=__init__.py
